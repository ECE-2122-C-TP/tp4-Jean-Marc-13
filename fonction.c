//
// Created by User on 29-09-21.
//

#include "fonction.h"


int getMax(int ent1, int ent2) {
    if (ent1>ent2) {
        return ent1;
    }
    else {
        return ent2;
    }
}

int saisirEntier() {
    int entier = 0;
    printf("Saisissez un entier : \n");
    scanf("%d, entier");
    return entier;
}

int calcullAire(longueur, largeur) {
    printf("l'aire du rectangle est :" %d, longueur * largeur);
}

int calculPerimetre(longueur, largeur) {
    printf("le périmètre du rectangle est :" %d, (longueur + largeur)/2);
}

int testMultiple(entier1,entier2) {
    if (entier1 % entier2 == 0) { return 1; }
        else {return 0; }
}

